import { FigureType } from "@/types/figure";

export const FIGURE_AVATAR_PATH = "/figures/";

/**
 * A figure to fill empty tiles on the field
 */
export const NullFigure: FigureType = {type: null, color: null};